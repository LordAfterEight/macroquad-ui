use macroquad::prelude::*;

#[derive(Default, Debug)]
pub struct Slider {
    pub x: f32,
    pub y: f32,
    pub w: f32,
    pub y: f32,
    pub value: f32,
    pub fg_col: Color,
    pub bg_col: Color,
    pub text_col: Color,
    pub hover_col: Color,
    pub click_col: Color,
}

impl Slider {
}
